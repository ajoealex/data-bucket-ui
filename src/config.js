// Environment configuration for Data Bucket UI
export const config = {
  // Community server URL - if empty, only show regular login
  // communityServerUrl:  'https://databucket.automationhub.in',
  communityServerUrl:  'http://127.0.0.1:7895',

  // Maximum number of buckets per user/browser
  maxBucketsPerUser: 5,
};